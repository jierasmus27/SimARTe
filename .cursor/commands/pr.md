/pr When you see this command, please run rubocop and eslint to make sure that everything is up to standard. If not, please fix any issues. Otherwise, if linting passed, check the prompt. By default, group all unstaged files into sensible commits with descriptive commit messages. If I said something like /pr current, skip this commit step. Then create a PR in Github via the CLI. Name the PR to a descriptive name for the feature, add a description of the change with some quick test examples in the Github description (in proper Markdown). Assign the user to the PR. Please and thank you.
