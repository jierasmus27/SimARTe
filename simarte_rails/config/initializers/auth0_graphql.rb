# frozen_string_literal: true

# Auth0 settings for GraphQL Bearer JWT verification (see Auth0AccessTokenVerifier).
# Prefer ENV["AUTH0_DOMAIN"] / ENV["AUTH0_AUDIENCE"] or
# Rails credentials: auth0.domain, auth0.audience (see openspec/specs/auth0-graphql-jwt.md).
