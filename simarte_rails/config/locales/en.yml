en:
  activerecord:
    attributes:
      user:
        role:
          descriptions:
            user: "Basic role for using selected services"
            admin: "Access to the admin panel and all services"
